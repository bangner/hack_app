<!DOCTYPE html>
<html lang="en">

  <head>
    <title>hack_app - <%= yield :title %></title>

    <meta charset="UTF-8" />
    <meta name="description" content="hack_app (hack app) is dedicated to improving the coding bootcamp industry. For students, we will provide information on bootcamps, alongside a common application process. For bootcamps, we will provide better information on students." />
    <%= yield :meta %>
    
    <%= favicon_link_tag 'favicon.ico' %>

    <%= stylesheet_link_tag "//cloud.typography.com/7222252/625882/css/fonts.css", media: "all" %>
    <%= stylesheet_link_tag "public", media: "all" %>
    <%= stylesheet_link_tag "admin", media: "all" if admin_namespace? %>
    <%= yield :styles %>
  </head>

  <body class="<%= "admin" if admin_namespace? %> <%= controller_name %> <%= action_name %>">

    <%= render "shared/flash" %>

    <div class="site-wrap">
      <%= render "shared/header" %>
      <%= render "admin/shared/menu" if admin_namespace? %>
      <section class="site-content">
        <%= yield %>
      </section>
    </div>

    <%= render "shared/footer" unless admin_namespace? %>

    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
      ga('create', 'UA-44872904-1', 'hackapp.co');
      ga('send', 'pageview');
    </script>
    <%= javascript_include_tag "public" %>
    <%= javascript_include_tag "admin" if admin_namespace? %>
    <%= yield :scripts %>

  </body>
</html>
